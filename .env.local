# Configurações para desenvolvimento local
# Este arquivo sobrescreve o .env quando roda pnpm dev localmente

# Database - usa localhost ao invés de postgres (nome do serviço Docker)
DATABASE_URL=postgresql://podiogo:podiogo123@localhost:5432/podiogo?schema=public&connection_limit=20&pool_timeout=20

# Redis - usa localhost ao invés de redis (nome do serviço Docker)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_ENABLED=true
REDIS_PASSWORD=podiogo123
